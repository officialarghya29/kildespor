Run artifacts (profiles, summaries) are produced by `uv run kildespor run`
and validated by `uv run kildespor validate`. They are excluded from git
because each run must be generated by the exact committed code so that
every fact's evidence hash is verifiable against this tree.
