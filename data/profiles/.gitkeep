Committed run artifacts (profiles + _summary.json) are produced by
`uv run kildespor run` and validated by `uv run kildespor validate`
in CI. They must be generated by the exact committed code so that every
fact's evidence hash is verifiable against this tree. Fresh artifacts
that pass validation may be committed; stale ones are removed.
